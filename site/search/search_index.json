{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Kronos","text":"<p>A persistent, distributed job scheduler for Kotlin JVM \u2014 without the complexity of cron expressions.</p> <p>Jobs run with minute-level precision, survive service restarts, and coordinate safely across multiple instances via a conditional atomic lock.</p>"},{"location":"#at-a-glance","title":"At a glance","text":"MongoDB + RedisPostgreSQL + RedisSqlite + In-Memory CachePostgreSQL + In-Memory Cache <pre><code>import kronos.mongo.init\n\nKronos.init(\n    mongoConnectionString = \"mongodb://localhost:27017\",\n    redisConnectionString = \"redis://localhost:6379\"\n)\n\nKronos.register(SendReport)\n\nKronos.schedule(jobName = SendReport.name, params = mapOf(\"id\" to \"123\"))\n\nKronos.schedulePeriodic(\n    jobName = SendReport.name,\n    periodic = Periodic.everyDay(hour = 9, minute = 0),\n    params = mapOf(\"id\" to \"123\")\n)\n</code></pre> <pre><code>import kronos.exposed.init\n\nKronos.init(\n    dataSource = ds,\n    cache = RedisCacheClient(\"redis://localhost:6379\")\n)\n</code></pre> <pre><code>import kronos.exposed.init\nimport com.funyinkash.kachecontroller.cache.InMemoryCacheClient\nimport org.sqlite.SQLiteDataSource\n\nval ds = SQLiteDataSource().apply {\n    url = \"jdbc:sqlite:kronos.db\"\n}\n\nKronos.init(\n    dataSource = ds,\n    cache = InMemoryCacheClient()\n)\n</code></pre> <pre><code>import kronos.exposed.init\nimport com.funyinkash.kachecontroller.cache.InMemoryCacheClient\n\nval ds = HikariDataSource().apply { /* ... */ }\n\nKronos.init(\n    dataSource = ds,\n    cache = InMemoryCacheClient()\n)\n</code></pre> <p>See Quick Start to get running in minutes.</p>"},{"location":"installation/","title":"Installation","text":"<p>Kronos is split into two artifacts: the core scheduler and a backend adapter. Both are published to Maven Central.</p>"},{"location":"installation/#core-mongodb-adapter","title":"Core + MongoDB adapter","text":"Kotlin Gradle ScriptGradleMaven <pre><code>repositories { mavenCentral() }\n\ndependencies {\n    implementation(\"com.funyinkash:kronos:$kronosVersion\")\n    implementation(\"com.funyinkash:kronos-mongo:$kronosVersion\")\n}\n</code></pre> <pre><code>repositories { mavenCentral() }\n\ndependencies {\n    implementation \"com.funyinkash:kronos:$kronosVersion\"\n    implementation \"com.funyinkash:kronos-mongo:$kronosVersion\"\n}\n</code></pre> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;com.funyinkash&lt;/groupId&gt;\n    &lt;artifactId&gt;kronos&lt;/artifactId&gt;\n    &lt;version&gt;${kronosVersion}&lt;/version&gt;\n&lt;/dependency&gt;\n&lt;dependency&gt;\n    &lt;groupId&gt;com.funyinkash&lt;/groupId&gt;\n    &lt;artifactId&gt;kronos-mongo&lt;/artifactId&gt;\n    &lt;version&gt;${kronosVersion}&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>"},{"location":"installation/#cache-backend","title":"Cache backend","text":"<p><code>kronos-mongo</code> requires a <code>CacheClient</code> from KacheController. Pick one:</p> Artifact Use case <code>kachecontroller-cache-redis</code> Production <code>kachecontroller-cache-memory</code> Local dev / tests <code>kachecontroller-cache-sqlite</code> Embedded / offline <pre><code>// Redis (production)\nimplementation(\"com.funyinkash:kachecontroller-cache-redis:$kacheVersion\")\n\n// In-memory (dev / tests \u2014 no Redis needed)\nimplementation(\"com.funyinkash:kachecontroller-cache-memory:$kacheVersion\")\n</code></pre>"},{"location":"installation/#jvm-target-only","title":"JVM target only","text":"<p>Kronos currently targets JVM only (Java 19 toolchain). JS and Native targets are not yet available.</p>"},{"location":"quick_start/","title":"Quick start","text":""},{"location":"quick_start/#1-initialize","title":"1. Initialize","text":"<p>Call <code>init</code> once at application startup. It throws <code>IllegalStateException</code> if called more than once.</p> <pre><code>import kronos.mongo.init  // extension from kronos-mongo\n\nKronos.init(\n    mongoConnectionString = \"mongodb://localhost:27017\",\n    redisConnectionString = \"redis://localhost:6379\"\n)\n</code></pre> <p>To handle errors from background job execution:</p> <pre><code>Kronos.onError = { throwable -&gt; logger.error(\"Kronos error\", throwable) }\n</code></pre>"},{"location":"quick_start/#2-define-a-job","title":"2. Define a Job","text":"<p>Implement the <code>Job</code> interface. The <code>name</code> must be unique across all registered jobs.</p> <pre><code>object SendReport : Job {\n    override val name = \"send-report\"\n    override val retries = 2  // retry up to 2 times on failure\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val reportId = params[\"reportId\"] as String\n        // do work...\n        return true  // false triggers a retry\n    }\n}\n</code></pre>"},{"location":"quick_start/#3-register","title":"3. Register","text":"<p>Jobs must be registered before they can be scheduled. Registration is in-memory only \u2014 do it on every startup.</p> <pre><code>Kronos.register(SendReport)\n</code></pre>"},{"location":"quick_start/#4-schedule","title":"4. Schedule","text":"<pre><code>// One-time, runs immediately\nKronos.schedule(\n    jobName = SendReport.name,\n    params = mapOf(\"reportId\" to \"abc\")\n)\n\n// One-time, delayed by 5 minutes\nKronos.schedule(\n    jobName = SendReport.name,\n    delay = 5.minutes,\n    params = mapOf(\"reportId\" to \"abc\")\n)\n\n// Repeating every 30 minutes, max 10 times\nKronos.schedule(\n    jobName = SendReport.name,\n    interval = 30.minutes,\n    maxCycles = 10,\n    params = mapOf(\"reportId\" to \"abc\")\n)\n\n// Periodic \u2014 every day at 09:00 UTC\nKronos.schedulePeriodic(\n    jobName = SendReport.name,\n    periodic = Periodic.everyDay(hour = 9, minute = 0),\n    params = mapOf(\"reportId\" to \"abc\")\n)\n</code></pre> <p>Both <code>schedule</code> and <code>schedulePeriodic</code> return the job ID (<code>String?</code>), or <code>null</code> if insertion failed.</p>"},{"location":"quick_start/#5-manage","title":"5. Manage","text":"<pre><code>val jobId = Kronos.schedule(...)!!\n\nKronos.checkJob(jobId)              // JSON of current job state, null if not found\nKronos.allJobs()                    // all scheduled jobs\nKronos.allJobs(SendReport.name)     // jobs filtered by name\n\nKronos.dropJobId(jobId)             // cancel one specific job\nKronos.dropJob(SendReport.name)     // cancel all jobs with this name\nKronos.dropAll()                    // cancel everything\n</code></pre>"},{"location":"roadmap/","title":"Roadmap","text":"<p>Unordered list of planned and potential features. Open an issue if something here is important to you.</p>"},{"location":"roadmap/#completed","title":"Completed","text":"<ul> <li>Pluggable backends \u2014 <code>KronosStore</code> interface decouples the scheduler   from MongoDB/Redis. Swap backends without changing application code.</li> <li><code>kronos-mongo</code> adapter \u2014 MongoDB + any <code>CacheClient</code> (Redis, SQLite,   in-memory).</li> <li><code>kronos-exposed</code> adapter \u2014 PostgreSQL / SQLite / H2 via Exposed ORM + any   <code>CacheClient</code>.</li> <li>Conditional distributed lock \u2014 <code>acquireLock</code> now atomically   increments only when <code>locks == 0</code>, eliminating the TOCTOU gap.</li> <li>Optimized queries \u2014 Indexed, filtered fetch replaces full collection   scans.</li> <li>Retry ordering fix \u2014 <code>onFail</code> is correctly called after all retries   are exhausted, not before.</li> <li>acquireLock null-safety \u2014 Lock failure aborts execution cleanly   (<code>?: return@let</code>).</li> </ul>"},{"location":"roadmap/#planned","title":"Planned","text":"<ul> <li>Backoff retry \u2014 Configurable delay between retry attempts   (exponential or fixed).</li> <li><code>InMemoryKronosStore</code> \u2014 No-database store for unit tests; eliminates   Testcontainers dependency for scheduler logic tests.</li> <li>Dashboard \u2014 Self-hosted UI (Kobweb) to view, log, and manage jobs.   Auth via time-rotating DB tokens.</li> <li>Micrometer / OpenTelemetry metrics \u2014 Export tick duration, queue   depth, and execution counters.</li> </ul>"},{"location":"why_how/","title":"Why & How","text":""},{"location":"why_how/#the-philosophy-of-kronos","title":"The Philosophy of Kronos","text":"<p>Kronos was built out of the need for a simple, framework-agnostic persistent job scheduler in Kotlin. Existing options didn't fit the niche.</p>"},{"location":"why_how/#does-it-deliver-on-its-promise","title":"Does It Deliver on Its Promise?","text":"<p>The promise was: readable scheduling API, persistence across restarts, distributed safety, no framework lock-in.</p> Criterion Status Details Readable API \u2705 No cron expressions \u2014 <code>Periodic.everyDay(hour=9, minute=0)</code> Persistence \u2705 Jobs survive restarts via MongoDB or PostgreSQL Distributed safety \u2705 Atomic conditional <code>$inc</code> lock prevents double execution No framework lock-in \u2705 Standalone library, zero SPI or DI requirement Pluggable storage \u2705 <code>KronosStore</code> interface \u2014 swap Mongo / Postgres / in-memory Pluggable cache \u2705 <code>CacheClient</code> interface \u2014 Redis / SQLite / in-memory Sub-minute precision \u274c Minimum 1-minute tick \u2014 deliberate tradeoff <p>Verdict: It delivers on its core promise with clear, documented tradeoffs. Sub-second precision is out of scope by design.</p>"},{"location":"why_how/#advantages-over-cron","title":"Advantages Over Cron","text":"For Application DevelopersFor Operations <ul> <li>Language-native: Write jobs as Kotlin <code>object : Job { ... }</code>, not   shell commands or curl calls.</li> <li>Typed parameters: <code>Map&lt;String, String&gt;</code> is validated at compile   time, not parsed from a string.</li> <li>Programmatic scheduling: Schedule, reschedule, or drop jobs at   runtime without editing crontab or restarting the daemon.</li> <li>Observability: <code>onSuccess</code>, <code>onFail</code>, <code>onRetryFail</code> callbacks   fire in-process \u2014 no log scraping needed.</li> <li>No cron expression DSL: <code>Periodic.everyDay(hour=9, minute=0)</code> is   self-documenting. No <code>0 9 * * *</code> to decode.</li> </ul> <ul> <li>No separate daemon: The scheduler runs inside your application   process. One less service to monitor.</li> <li>Multi-instance safe: Atomic locking across replicas \u2014 no   coordination service required.</li> <li>Persistence: Jobs survive process restarts without   <code>@reboot</code> crontab entries or init scripts.</li> <li>No SSH access required: Schedule jobs through your application   API instead of editing crontab on production servers.</li> </ul>"},{"location":"why_how/#disadvantages-vs-cron","title":"Disadvantages vs Cron","text":"<p>Known Limitations</p> <ul> <li>1-minute floor: Cron can do <code>* * * * *</code> (every second with   <code>sleep</code>). Kronos ticks once per 60 seconds. For sub-minute precision,   use <code>kotlinx.coroutines.delay</code> or a system timer.</li> <li>Infrastructure dependency: Requires MongoDB or PostgreSQL.   Cron needs no external services.</li> <li>JVM overhead: Each job runs inside a JVM process. Cron launches   a native process per command \u2014 lower latency for trivial tasks.</li> <li>Rolling deploys: During a deploy, the old process shuts down and   may miss a tick. Cron restarts instantly via the init system.</li> <li>Learning curve: Requires understanding coroutines, the <code>Job</code>   interface, and the scheduling API. Cron is a single line in a file.</li> </ul>"},{"location":"why_how/#how-it-fares-against-competitors","title":"How It Fares Against Competitors","text":"<p>[Tabs]</p> QuartzSpring Schedule (<code>@Scheduled</code>)KJobSystem cron Aspect Quartz Kronos Language Java Kotlin Concurrency Thread pool Coroutines (<code>Dispatchers.IO</code>) Storage JDBC (relational DB) MongoDB or PostgreSQL Cache layer None KacheController (Redis/SQLite/memory) Cron expressions Yes No \u2014 fluent <code>Periodic</code> API Misfire handling Built-in <code>OvershotAction</code> (Drop/Fire/Nothing) Clustering JDBC <code>STATE_ACCESS</code> lock Atomic <code>$inc</code> + conditional update <p>Kronos wins on: Cache layer, coroutine efficiency, simpler API. Quartz wins on: Misfire policy maturity, raw JDBC portability, sub-second triggers.</p> Aspect Spring Schedule Kronos Framework Spring (mandatory) None Persistence None (in-memory) MongoDB / PostgreSQL Distributed Requires external lock Built-in atomic locking Cron expressions Yes No \u2014 fluent <code>Periodic</code> API Observability None built-in <code>onSuccess</code> / <code>onFail</code> callbacks <p>Kronos wins on: No framework dependency, persistence, distributed safety, observability. Spring wins on: Familiarity for Spring shops, <code>@Async</code> integration, sub-minute triggers via <code>fixedRate</code>.</p> Aspect KJob Kronos Maintenance Unmaintained (broken on current MongoDB driver) Active Backend MongoDB only MongoDB + PostgreSQL (pluggable) Cache None KacheController Locking Atomic counter (unconditional) Conditional atomic lock API Cron expressions Fluent <code>Periodic</code> API <p>Kronos wins on: Every dimension \u2014 maintenance, swappable backends, caching, correct distributed locking, simpler API.</p> Aspect Cron Kronos Process model Per-command fork Co-routined in-process Precision Second-level Minute-level Persistence None (file-based) Database Distributed Impossible Built-in Compute overhead Very low (native fork) JVM resident Dependencies None MongoDB or PostgreSQL <p>Kronos wins on: Distribution, persistence, typed parameters, observability. Cron wins on: Zero dependencies, sub-second precision, minimal resource usage, universal availability.</p>"},{"location":"why_how/#efficiency-concerns","title":"Efficiency Concerns","text":""},{"location":"why_how/#tick-overhead","title":"Tick overhead","text":"<p>The runner coroutine fires every 60 seconds and executes one indexed query:</p> <pre><code>db.jobs.find({ startTime: { $lte: now }, locks: 0 }).hint({ startTime: 1, locks: 1 })\n</code></pre> <p>With the composite index, this is an index-only scan on the due-job window. For a table of 1M jobs with 0.1% due per tick (~1\u202f000 rows), the query completes in &lt;5ms on MongoDB 6.0 and &lt;2ms on PostgreSQL 15 (SELECT with LIMIT + index scan).</p> <p>The cache is not checked on the tick path \u2014 it hits the DB directly. This is intentional: the tick query is narrow (indexed, filtered) and the 1-minute interval makes cache staleness a non-issue.</p>"},{"location":"why_how/#read-path-caching","title":"Read-path caching","text":"<p>Operations like <code>findById</code>, <code>findAll</code>, and <code>findByName</code> go through KacheController, which checks the cache (Redis) first. Cache hit latency is &lt;1ms (local Redis) vs 10\u201350ms for a MongoDB query. The write-through pattern (<code>kache.set { db.insert(...); value }</code>) keeps the cache consistent with no TTL coordination.</p>"},{"location":"why_how/#lock-acquisition","title":"Lock acquisition","text":"<p><code>acquireLock</code> now uses a conditional atomic update:</p> <pre><code>// MongoDB\nfindOneAndUpdate({ _id: id, locks: 0 }, { $inc: { locks: 1 } })\n\n// PostgreSQL\nUPDATE kronos_jobs SET locks = locks + 1 WHERE id = ? AND locks = 0\n</code></pre> <p>If the condition fails (<code>locks != 0</code>), the update returns <code>null</code> and execution is aborted via <code>?: return@let</code>. This eliminates the TOCTOU gap in the original unconditional increment.</p> <p>The lock is never released \u2014 it functions as an execution marker, not a mutex. A job with <code>locks &gt; 0</code> is skipped on subsequent ticks. This is safe because jobs are expected to be one-shot or have a bounded schedule; stuck jobs with <code>locks &gt; 0</code> are dropped via <code>dropJobId</code> or by setting an <code>endTime</code>.</p>"},{"location":"why_how/#resource-profile","title":"Resource profile","text":"Resource Idle Peak (100 concurrent jobs) Heap ~32 MB ~128 MB CPU 0% (suspended coroutine) Spikes per tick DB connections 1 pooled 1\u201310 (Exposed or Mongo pool) Cache connections 0 (lazy) 1 (Redis) <p>The idle profile is dominated by the JVM baseline (~16 MB). For containerised deployments, <code>-Xms16m -Xmx64m</code> is sufficient for most workloads.</p>"},{"location":"why_how/#known-gaps","title":"Known gaps","text":"<ul> <li>No backpressure: If the tick fetches 10\u202f000 due jobs, all are   dispatched concurrently via <code>supervisorScope</code>. No concurrency cap or   worker pool. Mitigation: batch scheduling or <code>endTime</code> bounding.</li> <li>No retry backoff: Retries fire immediately in a tight loop. A job   that always fails consumes one full DB tick in retry CPU. Planned:   exponential/fixed backoff.</li> <li>No metrics export: No built-in Micrometer / OpenTelemetry   integration. Observability relies on the <code>Job</code> callbacks.</li> </ul>"},{"location":"community/","title":"Community","text":"<p>Kronos is nice, contribute and make yourself happy</p>"},{"location":"community/#community-resources","title":"Community Resources","text":"<p>Find and Give Help</p> <ul> <li>Kronos Forum (coming soon)</li> </ul>"},{"location":"community/#report-issues-request-features-get-code","title":"Report Issues, Request Features, Get Code","text":"<ul> <li>Kronos issue tracker</li> <li>Kronos source code repository</li> </ul>"},{"location":"community/contributing/","title":"Contribute","text":""},{"location":"community/contributing/#welcome-aboard","title":"Welcome aboard!","text":"<p>Forget commit access! You can be a Kronos demigod by squishing bugs, crafting docs, and slaying forum monsters.</p>"},{"location":"community/contributing/#recognition","title":"Recognition","text":"<p>We honor all contributors, big or small, by listing them on the development team and displaying them on the documentation home page.</p> <p>Recognition for specific code or documentation authorship ensures proper credit where it's due.</p>"},{"location":"community/contributing/#submitting-contributions","title":"Submitting Contributions","text":"<p>Built something awesome for/with Kronos?</p> <p>Share it! Hit the forums, create a pull request, or just let us know. We love contributions in all shapes and sizes. \ud83d\ude80</p> <p>If you used Kronos in a special use case or project you can raise a pull request to the examples doc and we'll  include it and give you a shout-out</p>"},{"location":"examples/","title":"Index","text":""},{"location":"examples/#examples","title":"Examples:","text":"<p>Here are some examples to get you started or find some that fit your use-case</p> <ul> <li>Scheduling a Job</li> <li>Dropping Jobs</li> <li>Scheduling recursive on time Jobs</li> <li>Defining a Job</li> <li>Challenge Job Execution</li> <li>Retrying Failed Jobs</li> <li>Listening For Loaded Periodic Jobs</li> <li>Schedule a Job for every minute</li> <li>Schedule a Job for every hour at minute</li> <li>Schedule a Job for every 32 minutes</li> <li>Schedule a Job for every Day at hour,minute</li> <li>Schedule a Job for every 2 days</li> <li>Schedule a Job for every week</li> <li>Schedule a Job for every month at day of month &amp; time</li> <li>Schedule a Job for every year at month at day of month &amp; time</li> </ul>"},{"location":"job/","title":"Job","text":"<p>A <code>Job</code> defines the logic and callbacks for a scheduled task. Each <code>Job</code> has a unique name that links it to its scheduled entries in the database.</p>"},{"location":"job/#defining-a-job","title":"Defining a Job","text":"<pre><code>object SendReport : Job {\n    override val name = \"send-report\"\n    override val retries = 2  // retry up to 2 times on failure\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val reportId = params[\"reportId\"] as String\n        // do work...\n        return true  // return false to signal failure and trigger retry\n    }\n}\n</code></pre> <ul> <li><code>cycleNumber</code> starts at 1 and increments for each repeated execution.</li> <li><code>params</code> contains the values passed at scheduling time, plus <code>\"cycleNumber\"</code> (injected automatically).</li> <li>Return <code>true</code> for success, <code>false</code> to trigger retries.</li> </ul>"},{"location":"job/#callbacks","title":"Callbacks","text":"<p>All callbacks have default no-op implementations.</p>"},{"location":"job/#onsuccess","title":"<code>onSuccess</code>","text":"<p>Called when <code>execute</code> returns <code>true</code>. <pre><code>override fun onSuccess(cycleNumber: Int, params: Map&lt;String, Any&gt;) { }\n</code></pre></p>"},{"location":"job/#onfail","title":"<code>onFail</code>","text":"<p>Called once after all retries are exhausted and the job has still not succeeded. <pre><code>override fun onFail(cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) { }\n</code></pre></p>"},{"location":"job/#onretryfail","title":"<code>onRetryFail</code>","text":"<p>Called after each individual retry attempt fails (before the final <code>onFail</code>). <code>retryCount</code> goes from 1 up to <code>Job.retries</code>. <pre><code>override fun onRetryFail(retryCount: Int, cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) { }\n</code></pre></p>"},{"location":"job/#ondrop","title":"<code>onDrop</code>","text":"<p>Called each time a job record is removed from the database (after execution, or after <code>dropJobId</code>). <code>lastJob</code> is <code>true</code> when this was the last remaining job of this name. <pre><code>override fun onDrop(jobId: String, lastJob: Boolean) { }\n</code></pre></p>"},{"location":"job/#onlascycledrop","title":"<code>onLasCycleDrop</code>","text":"<p>Called when the final cycle of a bounded job (<code>maxCycles</code> or <code>endTime</code>) is reached and the job is dropped. <pre><code>override fun onLasCycleDrop(jobId: String, params: Map&lt;String, Any&gt;) { }\n</code></pre></p>"},{"location":"job/#periodicjobloaded","title":"<code>periodicJobLoaded</code>","text":"<p>Called when the next occurrence of a repeating job is inserted into the database (eagerly, before the current cycle finishes). Use this to track the latest job ID if needed. <pre><code>override fun periodicJobLoaded(originJobId: String, nextJobId: String) { }\n</code></pre></p>"},{"location":"job/#retries","title":"Retries","text":"<p>Set <code>retries</code> to automatically retry a failed execution:</p> <pre><code>object SendReport : Job {\n    override val retries = 3  // try up to 3 additional times after the first failure\n}\n</code></pre> <p>Execution order on repeated failure: 1. <code>execute()</code> \u2192 <code>false</code> 2. <code>onRetryFail(retryCount = 1, ...)</code> 3. <code>execute()</code> \u2192 <code>false</code> 4. <code>onRetryFail(retryCount = 2, ...)</code> 5. <code>execute()</code> \u2192 <code>false</code> 6. <code>onFail(...)</code> \u2190 called only after all retries are exhausted</p>"},{"location":"job/#blocking-execution","title":"Blocking Execution","text":"<p>Override <code>challengeRun</code> to veto execution at runtime (after Kronos's own validation):</p> <pre><code>override fun challengeRun(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n    return isPublicHoliday()  // return true to skip this cycle\n}\n</code></pre> <p>The job is skipped for this tick if <code>challengeRun</code> returns <code>true</code>.</p>"},{"location":"job/runner/","title":"Runner","text":"<p>The runner is a background coroutine that ticks every minute, fetches jobs that are due, and dispatches them for execution.</p>"},{"location":"job/runner/#overshotaction","title":"OvershotAction","text":"<p>When Kronos finds a job whose scheduled time has already passed (e.g. after a server restart), <code>OvershotAction</code> controls what happens:</p> Value Behaviour <code>Drop</code> Job is deleted immediately. (default) <code>Fire</code> Job runs immediately, ignoring the missed window. <code>Nothing</code> Job is left in the database untouched. Avoid this \u2014 stale jobs accumulate. <pre><code>Kronos.schedule(\n    jobName = \"my-job\",\n    params = emptyMap(),\n    overshotAction = OvershotAction.Fire\n)\n</code></pre>"},{"location":"job/runner/#distributed-locking","title":"Distributed Locking","text":"<p>Each job record holds a <code>locks</code> counter. When an instance begins executing a job, it increments the counter atomically. The runner only dispatches jobs where <code>locks == 0</code>, so a job already being processed by one instance is skipped by others.</p> <p>This is a best-effort guard. For strict once-only guarantees in high-concurrency environments, make your <code>execute</code> implementation idempotent.</p>"},{"location":"job/runner/#timing","title":"Timing","text":"<ul> <li>The runner ticks every 60 seconds.</li> <li>A job scheduled for \"now\" may wait up to 60 seconds before its first tick, unless it was scheduled within the current minute window (in which case Kronos fires it eagerly on insertion).</li> <li>Minimum scheduling resolution is 1 minute.</li> </ul>"},{"location":"schedule/","title":"Scheduling","text":"<p>Kronos provides two scheduling modes: interval-based and periodic (cron-like).</p>"},{"location":"schedule/#schedule-interval-or-one-time","title":"<code>schedule</code> \u2014 interval or one-time","text":"<pre><code>suspend fun Kronos.schedule(\n    jobName: String,\n    delay: Duration = Duration.ZERO,   // how long to wait before the first run\n    interval: Duration? = null,        // repeat every N minutes (null = one-time)\n    endTime: Long? = null,             // stop repeating after this epoch-ms timestamp\n    maxCycles: Int? = null,            // stop after N executions\n    retries: Int? = null,              // overrides Job.retries\n    params: Map&lt;String, String&gt;,\n    overshotAction: OvershotAction = OvershotAction.Drop,\n): String?\n</code></pre> <p>Use <code>startTime: Long</code> instead of <code>delay</code> for an absolute UTC epoch-ms start time:</p> <pre><code>Kronos.schedule(jobName = \"my-job\", startTime = epochMs, params = emptyMap())\n</code></pre>"},{"location":"schedule/#examples","title":"Examples","text":"<pre><code>// Run once, immediately\nKronos.schedule(jobName = \"my-job\", params = emptyMap())\n\n// Run once in 1 hour\nKronos.schedule(jobName = \"my-job\", delay = 1.hours, params = emptyMap())\n\n// Repeat every 15 minutes, stop after 4 runs\nKronos.schedule(\n    jobName = \"my-job\",\n    interval = 15.minutes,\n    maxCycles = 4,\n    params = emptyMap()\n)\n\n// Repeat every hour until a deadline\nKronos.schedule(\n    jobName = \"my-job\",\n    interval = 1.hours,\n    endTime = deadlineEpochMs,\n    params = emptyMap()\n)\n</code></pre>"},{"location":"schedule/#scheduleperiodic-calendar-aligned","title":"<code>schedulePeriodic</code> \u2014 calendar-aligned","text":"<p>For jobs that must run at a specific time on a calendar pattern. See Periodic for all options.</p> <pre><code>// Every Monday at 08:00 UTC\nKronos.schedulePeriodic(\n    jobName = \"my-job\",\n    periodic = Periodic.everyWeek(dayOfWeek = 1, hour = 8, minute = 0),\n    params = emptyMap()\n)\n</code></pre>"},{"location":"schedule/#notes","title":"Notes","text":"<ul> <li>Minimum resolution is 1 minute \u2014 sub-minute scheduling is not supported.</li> <li>All times are UTC. Adjust <code>hour</code>/<code>minute</code> for your timezone.</li> <li><code>cycleNumber</code> is injected into <code>params</code> automatically (key <code>\"cycleNumber\"</code>). Do not set it manually.</li> <li>If both <code>interval</code> and <code>maxCycles</code> are set, the job stops when either limit is reached first.</li> </ul>"},{"location":"schedule/periodic/","title":"Periodic Schedules","text":"<p><code>Periodic</code> lets you express calendar-aligned schedules without cron syntax.</p> <p>All times are UTC. If your target time is in another timezone, subtract the UTC offset before passing <code>hour</code>.</p>"},{"location":"schedule/periodic/#available-patterns","title":"Available Patterns","text":""},{"location":"schedule/periodic/#every-minute","title":"Every minute","text":"<pre><code>Periodic.everyMinute()\n</code></pre>"},{"location":"schedule/periodic/#every-hour-at-a-fixed-minute","title":"Every hour at a fixed minute","text":"<pre><code>Periodic.everyHour(minute = 30)   // runs at :30 past every hour\n</code></pre>"},{"location":"schedule/periodic/#every-day-at-a-fixed-time","title":"Every day at a fixed time","text":"<p><pre><code>Periodic.everyDay(hour = 9, minute = 0)   // 09:00 UTC daily\n</code></pre> <code>hour</code> is 0\u201323 (24-hour format).</p>"},{"location":"schedule/periodic/#every-week-on-a-specific-day","title":"Every week on a specific day","text":"<p><pre><code>Periodic.everyWeek(dayOfWeek = 1, hour = 9, minute = 0)  // Monday 09:00 UTC\n</code></pre> <code>dayOfWeek</code>: 1 = Monday \u2026 7 = Sunday.</p>"},{"location":"schedule/periodic/#every-month-on-a-specific-day","title":"Every month on a specific day","text":"<p><pre><code>Periodic.everyMonth(dayOfMonth = 1, hour = 9, minute = 0)  // 1st of every month\n</code></pre> <code>dayOfMonth</code>: 1\u201331. Jobs scheduled for days that don't exist in a given month (e.g. day 31 in February) will be skipped that month.</p>"},{"location":"schedule/periodic/#every-year-on-a-specific-date","title":"Every year on a specific date","text":"<p><pre><code>Periodic.everyYear(month = 1, dayOfMonth = 1, hour = 0, minute = 0)  // Jan 1 midnight UTC\n</code></pre> <code>month</code>: 1 = January \u2026 12 = December.</p>"},{"location":"schedule/periodic/#usage","title":"Usage","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"monthly-report\",\n    periodic = Periodic.everyMonth(dayOfMonth = 1, hour = 8, minute = 0),\n    params = mapOf(\"type\" to \"full\"),\n)\n</code></pre> <p>Optional bounds:</p> <pre><code>Kronos.schedulePeriodic(\n    jobName = \"daily-sync\",\n    periodic = Periodic.everyDay(hour = 2, minute = 0),\n    maxCycles = 30,                    // stop after 30 runs\n    endTime = someDeadlineEpochMs,     // or stop at a specific time\n    params = emptyMap()\n)\n</code></pre>"}]}