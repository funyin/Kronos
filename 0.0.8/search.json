{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Kronos","text":"<p>A persistent, distributed job scheduler for Kotlin JVM — without the complexity of cron expressions.</p> <p>Jobs run with minute-level precision, survive service restarts, and coordinate safely across multiple instances via a conditional atomic lock.</p>","path":["Kronos"],"tags":[]},{"location":"#at-a-glance","level":2,"title":"At a glance","text":"MongoDB + RedisPostgreSQL + RedisSqlite + In-Memory CachePostgreSQL + In-Memory Cache <pre><code>import kronos.mongo.init\n\nKronos.init(\n    mongoConnectionString = \"mongodb://localhost:27017\",\n    redisConnectionString = \"redis://localhost:6379\"\n)\n\nKronos.register(SendReport)\n\nKronos.schedule(jobName = SendReport.name, params = mapOf(\"id\" to \"123\"))\n\nKronos.schedulePeriodic(\n    jobName = SendReport.name,\n    periodic = Periodic.everyDay(hour = 9, minute = 0),\n    params = mapOf(\"id\" to \"123\")\n)\n</code></pre> <pre><code>import kronos.exposed.init\n\nKronos.init(\n    dataSource = ds,\n    cache = RedisCacheClient(\"redis://localhost:6379\")\n)\n</code></pre> <pre><code>import kronos.exposed.init\nimport com.funyinkash.kachecontroller.cache.InMemoryCacheClient\nimport org.sqlite.SQLiteDataSource\n\nval ds = SQLiteDataSource().apply {\n    url = \"jdbc:sqlite:kronos.db\"\n}\n\nKronos.init(\n    dataSource = ds,\n    cache = InMemoryCacheClient()\n)\n</code></pre> <pre><code>import kronos.exposed.init\nimport com.funyinkash.kachecontroller.cache.InMemoryCacheClient\n\nval ds = HikariDataSource().apply { /* ... */ }\n\nKronos.init(\n    dataSource = ds,\n    cache = InMemoryCacheClient()\n)\n</code></pre> <p>See Quick Start to get running in minutes.</p>","path":["Kronos"],"tags":[]},{"location":"installation/","level":1,"title":"Installation","text":"<p>Kronos is split into a core framework and backend adapters. Choose a backend — the core is pulled in transitively. All artifacts are published to Maven Central.</p>","path":["Installation"],"tags":[]},{"location":"installation/#backend-adapters","level":2,"title":"Backend adapters","text":"<p>Latest version</p> <p>Replace <code>KRONOS_VERSION</code> with the latest release (currently 0.0.8) and <code>KACHE_VERSION</code> with the latest KacheController release (currently 1.0.6).</p> Kotlin Gradle ScriptGradleMaven <pre><code>repositories { mavenCentral() }\n\ndependencies {\n    // Pick one backend — the core kronos API is included automatically\n    implementation(\"com.funyinkash:kronos-mongo:KRONOS_VERSION\")\n    // or implementation(\"com.funyinkash:kronos-exposed:KRONOS_VERSION\")\n}\n</code></pre> <pre><code>repositories { mavenCentral() }\n\ndependencies {\n    implementation \"com.funyinkash:kronos-mongo:KRONOS_VERSION\"\n    // or implementation \"com.funyinkash:kronos-exposed:KRONOS_VERSION\"\n}\n</code></pre> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;com.funyinkash&lt;/groupId&gt;\n    &lt;artifactId&gt;kronos-mongo&lt;/artifactId&gt;\n    &lt;version&gt;KRONOS_VERSION&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#cache-backend","level":2,"title":"Cache backend","text":"<p><code>kronos-mongo</code> and <code>kronos-exposed</code> require a <code>CacheClient</code> from KacheController. Pick one:</p> Artifact Use case <code>kachecontroller-cache-redis</code> Production <code>kachecontroller-cache-memory</code> Single-instance deployments, local dev, and tests (no Redis needed) <pre><code>// Redis (production / multi-instance)\nimplementation(\"com.funyinkash:kachecontroller-cache-redis:KACHE_VERSION\")\n\n// In-memory (single-instance, local dev / tests — no Redis needed)\nimplementation(\"com.funyinkash:kachecontroller-cache-memory:KACHE_VERSION\")\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#using-with-maven","level":2,"title":"Using with Maven","text":"<pre><code>&lt;dependencies&gt;\n    &lt;dependency&gt;\n        &lt;groupId&gt;com.funyinkash&lt;/groupId&gt;\n        &lt;artifactId&gt;kronos-mongo&lt;/artifactId&gt;\n        &lt;version&gt;KRONOS_VERSION&lt;/version&gt;\n    &lt;/dependency&gt;\n    &lt;dependency&gt;\n        &lt;groupId&gt;com.funyinkash&lt;/groupId&gt;\n        &lt;artifactId&gt;kachecontroller-cache-redis&lt;/artifactId&gt;\n        &lt;version&gt;KACHE_VERSION&lt;/version&gt;\n    &lt;/dependency&gt;\n&lt;/dependencies&gt;\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#jvm-target-only","level":2,"title":"JVM target only","text":"<p>Kronos currently targets JVM only (Java 19 toolchain). JS and Native targets are not yet available.</p>","path":["Installation"],"tags":[]},{"location":"installation/#architecture","level":2,"title":"Architecture","text":"<p>Kronos follows a pluggable-backend pattern:</p> Artifact Role <code>kronos</code> / <code>kronos-jvm</code> Core framework — <code>Kronos</code> singleton, <code>KronosStore</code> interface, <code>Job</code> interface, scheduling, runner, execution <code>kronos-mongo</code> MongoDB <code>KronosStore</code> implementation + convenience <code>Kronos.init(mongoUri, redisUri)</code> <code>kronos-exposed</code> SQL <code>KronosStore</code> implementation via JetBrains Exposed + convenience <code>Kronos.init(dataSource, cache)</code> <p>The core types (<code>Kronos</code>, <code>Job</code>, <code>KronoJob</code>, etc.) are exposed transitively via <code>kronos-mongo</code> and <code>kronos-exposed</code> at <code>compile</code> scope. You only need to add the backend dependency. If you're implementing a custom <code>KronosStore</code>, depend on <code>kronos</code> directly.</p>","path":["Installation"],"tags":[]},{"location":"quick_start/","level":1,"title":"Quick start","text":"","path":["Quick start"],"tags":[]},{"location":"quick_start/#1-initialize","level":2,"title":"1. Initialize","text":"<p>Call <code>init</code> once at application startup. It throws <code>IllegalStateException</code> if called more than once.</p> <pre><code>import kronos.mongo.init  // extension from kronos-mongo\n\nKronos.init(\n    mongoConnectionString = \"mongodb://localhost:27017\",\n    redisConnectionString = \"redis://localhost:6379\"\n)\n</code></pre> <p>To handle errors from background job execution:</p> <pre><code>Kronos.onError = { throwable -&gt; logger.error(\"Kronos error\", throwable) }\n</code></pre>","path":["Quick start"],"tags":[]},{"location":"quick_start/#2-define-a-job","level":2,"title":"2. Define a Job","text":"<p>Implement the <code>Job</code> interface. The <code>name</code> must be unique across all registered jobs.</p> <pre><code>object SendReport : Job {\n    override val name = \"send-report\"\n    override val retries = 2  // retry up to 2 times on failure\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val reportId = params[\"reportId\"] as String\n        // do work...\n        return true  // false triggers a retry\n    }\n}\n</code></pre>","path":["Quick start"],"tags":[]},{"location":"quick_start/#3-register","level":2,"title":"3. Register","text":"<p>Jobs must be registered before they can be scheduled. Registration is in-memory only — do it on every startup.</p> <pre><code>Kronos.register(SendReport)\n</code></pre>","path":["Quick start"],"tags":[]},{"location":"quick_start/#4-schedule","level":2,"title":"4. Schedule","text":"<pre><code>// One-time, runs immediately\nKronos.schedule(\n    jobName = SendReport.name,\n    params = mapOf(\"reportId\" to \"abc\")\n)\n\n// One-time, delayed by 5 minutes\nKronos.schedule(\n    jobName = SendReport.name,\n    delay = 5.minutes,\n    params = mapOf(\"reportId\" to \"abc\")\n)\n\n// Repeating every 30 minutes, max 10 times\nKronos.schedule(\n    jobName = SendReport.name,\n    interval = 30.minutes,\n    maxCycles = 10,\n    params = mapOf(\"reportId\" to \"abc\")\n)\n\n// Periodic — every day at 09:00 UTC\nKronos.schedulePeriodic(\n    jobName = SendReport.name,\n    periodic = Periodic.everyDay(hour = 9, minute = 0),\n    params = mapOf(\"reportId\" to \"abc\")\n)\n</code></pre> <p>Both <code>schedule</code> and <code>schedulePeriodic</code> return the job ID (<code>String?</code>), or <code>null</code> if insertion failed.</p>","path":["Quick start"],"tags":[]},{"location":"quick_start/#5-manage","level":2,"title":"5. Manage","text":"<pre><code>val jobId = Kronos.schedule(...)!!\n\nKronos.checkJob(jobId)              // JSON of current job state, null if not found\nKronos.allJobs()                    // all scheduled jobs\nKronos.allJobs(SendReport.name)     // jobs filtered by name\n\nKronos.dropJobId(jobId)             // cancel one specific job\nKronos.dropJob(SendReport.name)     // cancel all jobs with this name\nKronos.dropAll()                    // cancel everything\n</code></pre>","path":["Quick start"],"tags":[]},{"location":"roadmap/","level":1,"title":"Roadmap","text":"<p>Unordered list of planned and potential features. Open an issue if something here is important to you.</p>","path":["Roadmap"],"tags":[]},{"location":"roadmap/#completed","level":2,"title":"Completed","text":"<ul> <li>Pluggable backends — <code>KronosStore</code> interface decouples the scheduler   from MongoDB/Redis. Swap backends without changing application code.</li> <li><code>kronos-mongo</code> adapter — MongoDB + any <code>CacheClient</code> (Redis, SQLite,   in-memory).</li> <li><code>kronos-exposed</code> adapter — PostgreSQL / SQLite / H2 via Exposed ORM + any   <code>CacheClient</code>.</li> <li>Conditional distributed lock — <code>acquireLock</code> now atomically   increments only when <code>locks == 0</code>, eliminating the TOCTOU gap.</li> <li>Optimized queries — Indexed, filtered fetch replaces full collection   scans.</li> <li>Retry ordering fix — <code>onFail</code> is correctly called after all retries   are exhausted, not before.</li> <li>acquireLock null-safety — Lock failure aborts execution cleanly   (<code>?: return@let</code>).</li> </ul>","path":["Roadmap"],"tags":[]},{"location":"roadmap/#planned","level":2,"title":"Planned","text":"<ul> <li>Backoff retry — Configurable fixed or exponential delay between retry   attempts. Currently retries fire in a tight loop with no delay.</li> <li>SQL locking fix — <code>ExposedKronosStore.acquireLock</code> should return <code>null</code>   when the conditional UPDATE affects 0 rows, matching the MongoDB backend's   behaviour and making the SQL backend safe under concurrent execution.</li> <li><code>InMemoryKronosStore</code> — No-database store for unit tests; eliminates   Testcontainers dependency for scheduler logic tests.</li> <li>Kronos Service — Standalone Docker image that exposes Kronos over a   REST API, enabling non-JVM languages to use Kronos without embedding the   library. Clients register jobs and receive execution callbacks via HTTP   webhooks. Official SDKs planned for Python, Node.js, Go, and Ruby.</li> <li>KronoWatch — Commercial SaaS for monitoring and managing Kronos jobs.   Visualise job history, configure failure alerts, and manage schedules across   services. Separate product from the open-source library and Docker service.</li> </ul>","path":["Roadmap"],"tags":[]},{"location":"why_how/","level":1,"title":"Why & How","text":"","path":["Why & How"],"tags":[]},{"location":"why_how/#the-philosophy-of-kronos","level":2,"title":"The Philosophy of Kronos","text":"<p>Kronos was built out of the need for a simple, framework-agnostic persistent job scheduler in Kotlin. Existing options didn't fit the niche.</p>","path":["Why & How"],"tags":[]},{"location":"why_how/#does-it-deliver-on-its-promise","level":2,"title":"Does It Deliver on Its Promise?","text":"<p>The promise was: readable scheduling API, persistence across restarts, distributed safety, no framework lock-in.</p> Criterion Status Details Readable API ✅ No cron expressions — <code>Periodic.everyDay(hour=9, minute=0)</code> Persistence ✅ Jobs survive restarts via MongoDB or PostgreSQL Distributed safety ✅ Atomic conditional lock prevents double execution No framework lock-in ✅ Standalone library, zero SPI or DI requirement Pluggable storage ✅ <code>KronosStore</code> interface — swap Mongo / Postgres / in-memory Pluggable cache ✅ <code>CacheClient</code> interface — Redis / SQLite / in-memory Sub-minute precision ❌ Minimum 1-minute tick — deliberate tradeoff <p>Verdict: It delivers on its core promise with clear, documented tradeoffs. Sub-second precision is out of scope by design.</p>","path":["Why & How"],"tags":[]},{"location":"why_how/#advantages-over-cron","level":2,"title":"Advantages Over Cron","text":"For Application DevelopersFor Operations <ul> <li>Language-native: Write jobs as Kotlin <code>object : Job { ... }</code>, not   shell commands or curl calls.</li> <li>Typed parameters: <code>Map&lt;String, String&gt;</code> is validated at compile   time, not parsed from a string.</li> <li>Programmatic scheduling: Schedule, reschedule, or drop jobs at   runtime without editing crontab or restarting the daemon.</li> <li>Observability: <code>onSuccess</code>, <code>onFail</code>, <code>onRetryFail</code> callbacks   fire in-process — no log scraping needed.</li> <li>No cron expression DSL: <code>Periodic.everyDay(hour=9, minute=0)</code> is   self-documenting. No <code>0 9 * * *</code> to decode.</li> </ul> <ul> <li>No separate daemon: The scheduler runs inside your application   process. One less service to monitor.</li> <li>Multi-instance safe: Atomic locking across replicas — no   coordination service required.</li> <li>Persistence: Jobs survive process restarts without   <code>@reboot</code> crontab entries or init scripts.</li> <li>No SSH access required: Schedule jobs through your application   API instead of editing crontab on production servers.</li> </ul>","path":["Why & How"],"tags":[]},{"location":"why_how/#disadvantages-vs-cron","level":2,"title":"Disadvantages vs Cron","text":"<p>Known Limitations</p> <ul> <li>1-minute floor: Cron can do <code>* * * * *</code> (every second with   <code>sleep</code>). Kronos ticks once per 60 seconds. For sub-minute precision,   use <code>kotlinx.coroutines.delay</code> or a system timer.</li> <li>Infrastructure dependency: Requires MongoDB or PostgreSQL.   Cron needs no external services.</li> <li>JVM overhead: Each job runs inside a JVM process. Cron launches   a native process per command — lower latency for trivial tasks.</li> <li>Rolling deploys: During a deploy, the old process shuts down and   may miss a tick. Cron restarts instantly via the init system.</li> <li>Learning curve: Requires understanding coroutines, the <code>Job</code>   interface, and the scheduling API. Cron is a single line in a file.</li> </ul>","path":["Why & How"],"tags":[]},{"location":"why_how/#how-it-fares-against-competitors","level":2,"title":"How It Fares Against Competitors","text":"QuartzSpring Schedule (<code>@Scheduled</code>)KJobSystem cron Aspect Quartz Kronos Language Java Kotlin Concurrency Thread pool Coroutines (<code>Dispatchers.IO</code>) Storage JDBC (relational DB) MongoDB or PostgreSQL Cache layer None KacheController (Redis/SQLite/memory) Cron expressions Yes No — fluent <code>Periodic</code> API Misfire handling Built-in <code>OvershotAction</code> (Drop/Fire/Nothing) Clustering JDBC <code>STATE_ACCESS</code> lock Atomic <code>$inc</code> + conditional update <p>Kronos wins on: Cache layer, coroutine efficiency, simpler API. Quartz wins on: Misfire policy maturity, raw JDBC portability, sub-second triggers.</p> Aspect Spring Schedule Kronos Framework Spring (mandatory) None Persistence None (in-memory) MongoDB / PostgreSQL Distributed Requires external lock Built-in atomic locking Cron expressions Yes No — fluent <code>Periodic</code> API Observability None built-in <code>onSuccess</code> / <code>onFail</code> callbacks <p>Kronos wins on: No framework dependency, persistence, distributed safety, observability. Spring wins on: Familiarity for Spring shops, <code>@Async</code> integration, sub-minute triggers via <code>fixedRate</code>.</p> Aspect KJob Kronos Maintenance Unmaintained (broken on current MongoDB driver) Active Backend MongoDB only MongoDB + PostgreSQL (pluggable) Cache None KacheController Locking Atomic counter (unconditional) Conditional atomic lock API Cron expressions Fluent <code>Periodic</code> API <p>Kronos wins on: Every dimension — maintenance, swappable backends, caching, correct distributed locking, simpler API.</p> Aspect Cron Kronos Process model Per-command fork Co-routined in-process Precision Second-level Minute-level Persistence None (file-based) Database Distributed Impossible Built-in Compute overhead Very low (native fork) JVM resident Dependencies None MongoDB or PostgreSQL <p>Kronos wins on: Distribution, persistence, typed parameters, observability. Cron wins on: Zero dependencies, sub-second precision, minimal resource usage, universal availability.</p>","path":["Why & How"],"tags":[]},{"location":"why_how/#efficiency","level":2,"title":"Efficiency","text":"","path":["Why & How"],"tags":[]},{"location":"why_how/#tick-flow","level":3,"title":"Tick flow","text":"<p>Every 60 seconds the runner fires a single indexed query:</p> <pre><code>-- MongoDB\ndb.jobs.find({ startTime: { $lte: now }, locks: 0 })\n\n-- SQL\nSELECT * FROM kronos_jobs WHERE start_time &lt;= ? AND locks = 0\n</code></pre> <p>Both backends create a composite index on <code>(start_time, locks)</code> at initialization, so the query is a narrow index range scan over the due-job window rather than a full table scan.</p> <p>For periodic jobs, the query returns all jobs whose <code>startTime</code> is in the past and whose <code>locks</code> counter is zero. A second validation pass in the runner then checks whether the job's calendar pattern (hour, minute, day-of-week, etc.) matches the current UTC minute before dispatching. This two-stage approach keeps the DB query simple while supporting complex periodic patterns in application code.</p>","path":["Why & How"],"tags":[]},{"location":"why_how/#read-path-caching","level":3,"title":"Read-path caching","text":"<p><code>findById</code>, <code>findAll</code>, and <code>findByName</code> go through KacheController, which checks the cache (Redis or in-memory) first. The tick query (<code>fetchDueJobs</code>) bypasses the cache and always hits the DB — the 1-minute interval makes cache staleness a non-issue on the hot path, and the narrow indexed query is fast enough to not warrant caching.</p>","path":["Why & How"],"tags":[]},{"location":"why_how/#lock-acquisition","level":3,"title":"Lock acquisition","text":"<p>Kronos uses a conditional atomic increment to prevent duplicate execution across instances:</p> <pre><code>-- MongoDB (atomic, single round-trip)\nfindOneAndUpdate({ _id: id, locks: 0 }, { $inc: { locks: 1 } })\n→ returns the document before update, or null if locks ≠ 0\n\n-- SQL\nUPDATE kronos_jobs SET locks = locks + 1 WHERE id = ? AND locks = 0\n</code></pre> <p>On the MongoDB backend, <code>acquireLock</code> returns <code>null</code> when another instance already holds the lock — execution aborts cleanly. On the SQL backend, the <code>UPDATE</code> is equally atomic, but the current implementation returns the job row regardless of whether the update was applied, meaning the null-check guard in the runner is ineffective under contention. This is a known gap (see below).</p> <p>The lock is never released. It functions as a one-shot execution marker: a job with <code>locks &gt; 0</code> is permanently skipped by the runner, then dropped after execution completes.</p>","path":["Why & How"],"tags":[]},{"location":"why_how/#resource-profile","level":3,"title":"Resource profile","text":"<p>Measured against the MongoDB backend with a Redis cache, 100 concurrent trivial jobs (500 ms <code>delay</code>, no allocations), JVM max heap 2 GB:</p> Checkpoint Heap used Idle — after init, no jobs ~50 MB After scheduling 100 jobs ~66 MB Peak — during concurrent execution ~67 MB After execution settled ~67 MB <p>The ~17 MB delta between idle and 100 concurrent jobs reflects coroutines' low per-task overhead — a thread-based scheduler would consume roughly 1 MB of stack space per concurrent task. Real-world peaks depend on how much each job allocates during execution.</p> <p>The idle footprint (~50 MB) is driven by the MongoDB and Redis driver initialization, not Kronos itself. For containerised deployments, <code>-Xmx128m</code> is a safe floor for most workloads. Connection pool size is controlled by your MongoDB driver or <code>DataSource</code> configuration — Kronos does not manage it directly.</p> Resource Idle Active Heap ~50 MB ~67 MB (100 concurrent jobs) CPU ~0% (suspended coroutine) Spikes per tick DB connections Pool (driver-managed) Pool (driver-managed) Cache connections 0 (lazy) 1 (Redis)","path":["Why & How"],"tags":[]},{"location":"why_how/#known-gaps","level":2,"title":"Known Gaps","text":"<ul> <li> <p>No backpressure: If a tick returns 10 000 due jobs, all are dispatched   concurrently via <code>supervisorScope</code>. There is no concurrency cap or worker   pool. Mitigation: use <code>endTime</code> or <code>maxCycles</code> to bound job volume, or batch   scheduling on the application side.</p> </li> <li> <p>No retry backoff: Retries fire in a tight loop with no delay. A job that   always fails burns a full DB round-trip per retry within the same tick.   Planned: configurable fixed or exponential backoff.</p> </li> <li> <p>SQL locking gap: <code>ExposedKronosStore.acquireLock</code> performs an atomic   conditional <code>UPDATE</code> but then unconditionally SELECTs and returns the row,   so it can return a non-null result even when the lock was not acquired. Under   concurrent load on the SQL backend, two instances could execute the same job.   The MongoDB backend does not have this issue. Fix planned.</p> </li> <li> <p><code>InMemoryKronosStore</code> missing: No in-process store exists for unit tests.   Tests currently require Testcontainers (real MongoDB + Redis). Planned.</p> </li> </ul>","path":["Why & How"],"tags":[]},{"location":"community/","level":1,"title":"Community","text":"<p>Kronos is an open-source project and welcomes contributions of all kinds — bug reports, feature requests, documentation improvements, and code.</p>","path":["Community"],"tags":[]},{"location":"community/#get-help","level":2,"title":"Get Help","text":"<ul> <li>GitHub Issues — Bug reports and feature requests</li> <li>GitHub Discussions — Questions, ideas, and general conversation</li> <li>Source Code — Browse, fork, and contribute</li> </ul>","path":["Community"],"tags":[]},{"location":"community/#stay-updated","level":2,"title":"Stay Updated","text":"<ul> <li>Watch the GitHub repository for releases</li> <li>Follow @funyin_kash on X for announcements</li> </ul>","path":["Community"],"tags":[]},{"location":"community/#contribute","level":2,"title":"Contribute","text":"<p>See Contributing for guidelines on submitting pull requests and adding examples.</p>","path":["Community"],"tags":[]},{"location":"community/contributing/","level":1,"title":"Contribute","text":"","path":["Community","Contribute"],"tags":[]},{"location":"community/contributing/#welcome-aboard","level":2,"title":"Welcome aboard!","text":"<p>Forget commit access! You can be a Kronos demigod by squishing bugs, crafting docs, and slaying forum monsters.</p>","path":["Community","Contribute"],"tags":[]},{"location":"community/contributing/#recognition","level":2,"title":"Recognition","text":"<p>We honor all contributors, big or small, by listing them on the development team and displaying them on the documentation home page.</p> <p>Recognition for specific code or documentation authorship ensures proper credit where it's due.</p>","path":["Community","Contribute"],"tags":[]},{"location":"community/contributing/#submitting-contributions","level":2,"title":"Submitting Contributions","text":"<p>Built something awesome for/with Kronos?</p> <p>Share it! Hit the forums, create a pull request, or just let us know. We love contributions in all shapes and sizes. 🚀</p> <p>If you used Kronos in a special use case or project you can raise a pull request to the examples doc and we'll  include it and give you a shout-out</p>","path":["Community","Contribute"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"<p>Runnable code examples for the most common Kronos use cases.</p>","path":["Examples"],"tags":[]},{"location":"examples/#defining-a-job","level":2,"title":"Defining a Job","text":"<p>A <code>Job</code> is a Kotlin <code>object</code> (or class) that implements the <code>Job</code> interface. Override only the callbacks you need.</p> <pre><code>object SendReport : Job {\n    override val name = \"send-report\"\n    override val retries = 2\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val reportId = params[\"reportId\"] as String\n        sendReport(reportId)\n        return true          // return false to trigger a retry\n    }\n\n    override fun onSuccess(cycleNumber: Int, params: Map&lt;String, Any&gt;) {\n        println(\"Report sent on cycle $cycleNumber\")\n    }\n\n    override fun onRetryFail(retryCount: Int, cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) {\n        println(\"Attempt $retryCount failed: ${exception?.message}\")\n    }\n\n    override fun onFail(cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) {\n        alertOncall(\"send-report failed after all retries: ${exception?.message}\")\n    }\n\n    override fun onDrop(jobId: String, lastJob: Boolean) {\n        if (lastJob) println(\"No more send-report jobs queued\")\n    }\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#scheduling-a-job","level":2,"title":"Scheduling a Job","text":"<pre><code>Kronos.register(SendReport)\n\n// One-time, runs on the next tick\nKronos.schedule(\n    jobName = SendReport.name,\n    params = mapOf(\"reportId\" to \"rpt-001\")\n)\n\n// One-time, delayed by 1 hour\nKronos.schedule(\n    jobName = SendReport.name,\n    delay = 1.hours,\n    params = mapOf(\"reportId\" to \"rpt-002\")\n)\n\n// One-time, at an absolute UTC timestamp\nKronos.schedule(\n    jobName = SendReport.name,\n    startTime = ZonedDateTime.of(2024, 12, 1, 9, 0, 0, 0, ZoneOffset.UTC).toEpochSecond() * 1000,\n    params = mapOf(\"reportId\" to \"rpt-003\")\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#dropping-jobs","level":2,"title":"Dropping Jobs","text":"<pre><code>val jobId = Kronos.schedule(jobName = SendReport.name, params = emptyMap())!!\n\n// Cancel one specific job\nKronos.dropJobId(jobId)\n\n// Cancel all jobs with a given name\nKronos.dropJob(SendReport.name)\n\n// Cancel every scheduled job\nKronos.dropAll()\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#retrying-failed-jobs","level":2,"title":"Retrying Failed Jobs","text":"<p>Set <code>retries</code> on the <code>Job</code> object. Each failure calls <code>onRetryFail</code>; <code>onFail</code> fires only after all retries are exhausted.</p> <pre><code>object FlakyScraper : Job {\n    override val name = \"scraper\"\n    override val retries = 3   // 1 initial attempt + 3 retries = 4 total attempts\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        return try {\n            scrapeUrl(params[\"url\"] as String)\n            true\n        } catch (e: IOException) {\n            false   // triggers a retry\n        }\n    }\n\n    override fun onRetryFail(retryCount: Int, cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) {\n        println(\"Retry $retryCount/3 failed\")\n    }\n\n    override fun onFail(cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) {\n        println(\"All retries exhausted for ${params[\"url\"]}\")\n    }\n}\n</code></pre> <p>You can also override <code>retries</code> per-schedule call:</p> <pre><code>Kronos.schedule(\n    jobName = FlakyScraper.name,\n    retries = 5,               // overrides Job.retries for this instance only\n    params = mapOf(\"url\" to \"https://example.com\")\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#scheduling-recursively-inside-execute","level":2,"title":"Scheduling Recursively Inside execute()","text":"<p>A job can schedule new jobs from within its own <code>execute</code> — useful for fan-out or conditional follow-up work.</p> <pre><code>object ProcessBatch : Job {\n    override val name = \"process-batch\"\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val items = fetchNextBatch()\n\n        // Schedule a follow-up for each item\n        items.forEach { item -&gt;\n            Kronos.schedule(\n                jobName = SendNotification.name,\n                delay = 5.minutes,\n                params = mapOf(\"itemId\" to item.id)\n            )\n        }\n\n        // Reschedule self if more batches remain\n        if (hasMoreBatches()) {\n            Kronos.schedule(\n                jobName = name,\n                delay = 1.minutes,\n                params = params\n            )\n        }\n\n        return true\n    }\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#tracking-the-next-periodic-job-id","level":2,"title":"Tracking the Next Periodic Job ID","text":"<p><code>periodicJobLoaded</code> is called as soon as the next occurrence is inserted into the database — before the current cycle finishes executing. Use it to track the live job ID.</p> <pre><code>object DailySync : Job {\n    override val name = \"daily-sync\"\n\n    @Volatile var currentJobId: String? = null\n\n    override fun periodicJobLoaded(originJobId: String, nextJobId: String) {\n        currentJobId = nextJobId\n        println(\"Next daily-sync scheduled as $nextJobId\")\n    }\n}\n\n// To cancel the next pending run at any time:\nDailySync.currentJobId?.let { Kronos.dropJobId(it) }\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#challenge-job-execution","level":2,"title":"Challenge Job Execution","text":"<p><code>challengeRun</code> is a runtime gate called before each execution. Return <code>true</code> to skip the cycle, <code>false</code> to allow it. Use this for scheduling patterns that <code>Periodic</code> alone cannot express.</p>","path":["Examples"],"tags":[]},{"location":"examples/#skip-on-public-holidays","level":3,"title":"Skip on public holidays","text":"<pre><code>object Payroll : Job {\n    override val name = \"payroll\"\n\n    override fun challengeRun(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        return isPublicHoliday(LocalDate.now(ZoneOffset.UTC))\n    }\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#run-only-on-tuesdays-schedule-daily-gate-on-day","level":3,"title":"Run only on Tuesdays (schedule daily, gate on day)","text":"<pre><code>object TuesdayReport : Job {\n    override val name = \"tuesday-report\"\n\n    override fun challengeRun(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        return LocalDate.now(ZoneOffset.UTC).dayOfWeek != DayOfWeek.TUESDAY\n    }\n}\n\n// Schedule daily — challengeRun filters to Tuesdays only\nKronos.schedulePeriodic(\n    jobName = TuesdayReport.name,\n    periodic = Periodic.everyDay(hour = 9, minute = 0),\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#bi-weekly-run-every-other-cycle","level":3,"title":"Bi-weekly (run every other cycle)","text":"<pre><code>object BiWeeklyDigest : Job {\n    override val name = \"bi-weekly-digest\"\n\n    override fun challengeRun(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        return cycleNumber % 2 != 0    // skip odd cycles, run on even\n    }\n}\n\nKronos.schedulePeriodic(\n    jobName = BiWeeklyDigest.name,\n    periodic = Periodic.everyWeek(dayOfWeek = 1, hour = 8, minute = 0),\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#second-tuesday-of-the-month","level":3,"title":"Second Tuesday of the month","text":"<pre><code>object MonthlyTeamUpdate : Job {\n    override val name = \"monthly-team-update\"\n\n    override fun challengeRun(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val today = LocalDate.now(ZoneOffset.UTC)\n        val isTuesday = today.dayOfWeek == DayOfWeek.TUESDAY\n        val isSecondWeek = today.dayOfMonth in 8..14\n        return !(isTuesday &amp;&amp; isSecondWeek)\n    }\n}\n\n// Schedule weekly on Tuesday — challengeRun narrows it to the 2nd occurrence\nKronos.schedulePeriodic(\n    jobName = MonthlyTeamUpdate.name,\n    periodic = Periodic.everyWeek(dayOfWeek = 2, hour = 10, minute = 0),\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#periodic-schedule-examples","level":2,"title":"Periodic Schedule Examples","text":"","path":["Examples"],"tags":[]},{"location":"examples/#every-minute","level":3,"title":"Every minute","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"health-check\",\n    periodic = Periodic.everyMinute(),\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-hour-at-a-fixed-minute","level":3,"title":"Every hour at a fixed minute","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"cache-refresh\",\n    periodic = Periodic.everyHour(minute = 30),   // runs at :30 past every hour\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-32-minutes-interval-based-not-calendar-aligned","level":3,"title":"Every 32 minutes (interval-based, not calendar-aligned)","text":"<p>Use <code>schedule</code> with an <code>interval</code> for non-standard intervals that don't map to a clock pattern:</p> <pre><code>Kronos.schedule(\n    jobName = \"metrics-flush\",\n    interval = 32.minutes,\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-day-at-a-specific-time","level":3,"title":"Every day at a specific time","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"daily-report\",\n    periodic = Periodic.everyDay(hour = 9, minute = 0),   // 09:00 UTC\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-2-days","level":3,"title":"Every 2 days","text":"<pre><code>Kronos.schedule(\n    jobName = \"alternate-day-sync\",\n    interval = 48.hours,\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-week-on-a-specific-day","level":3,"title":"Every week on a specific day","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"weekly-digest\",\n    periodic = Periodic.everyWeek(dayOfWeek = 5, hour = 17, minute = 0),  // Friday 17:00 UTC\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-month-on-a-specific-day-and-time","level":3,"title":"Every month on a specific day and time","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"monthly-invoice\",\n    periodic = Periodic.everyMonth(dayOfMonth = 1, hour = 8, minute = 0),  // 1st of each month\n    params = mapOf(\"type\" to \"invoice\")\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#every-year-on-a-specific-date","level":3,"title":"Every year on a specific date","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"annual-audit\",\n    periodic = Periodic.everyYear(month = 1, dayOfMonth = 1, hour = 0, minute = 0),  // Jan 1 midnight UTC\n    params = emptyMap()\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"job/","level":1,"title":"Job","text":"<p>A <code>Job</code> defines the logic and callbacks for a scheduled task. Each <code>Job</code> has a unique name that links it to its scheduled entries in the database.</p>","path":["Job"],"tags":[]},{"location":"job/#defining-a-job","level":2,"title":"Defining a Job","text":"<pre><code>object SendReport : Job {\n    override val name = \"send-report\"\n    override val retries = 2  // retry up to 2 times on failure\n\n    override suspend fun execute(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n        val reportId = params[\"reportId\"] as String\n        // do work...\n        return true  // return false to signal failure and trigger retry\n    }\n}\n</code></pre> <ul> <li><code>cycleNumber</code> starts at 1 and increments for each repeated execution.</li> <li><code>params</code> contains the values passed at scheduling time, plus <code>\"cycleNumber\"</code> (injected automatically).</li> <li>Return <code>true</code> for success, <code>false</code> to trigger retries.</li> </ul>","path":["Job"],"tags":[]},{"location":"job/#callbacks","level":2,"title":"Callbacks","text":"<p>All callbacks have default no-op implementations.</p>","path":["Job"],"tags":[]},{"location":"job/#onsuccess","level":3,"title":"<code>onSuccess</code>","text":"<p>Called when <code>execute</code> returns <code>true</code>. <pre><code>override fun onSuccess(cycleNumber: Int, params: Map&lt;String, Any&gt;) { }\n</code></pre></p>","path":["Job"],"tags":[]},{"location":"job/#onfail","level":3,"title":"<code>onFail</code>","text":"<p>Called once after all retries are exhausted and the job has still not succeeded. <pre><code>override fun onFail(cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) { }\n</code></pre></p>","path":["Job"],"tags":[]},{"location":"job/#onretryfail","level":3,"title":"<code>onRetryFail</code>","text":"<p>Called after each individual retry attempt fails (before the final <code>onFail</code>). <code>retryCount</code> goes from 1 up to <code>Job.retries</code>. <pre><code>override fun onRetryFail(retryCount: Int, cycleNumber: Int, params: Map&lt;String, Any&gt;, exception: Exception?) { }\n</code></pre></p>","path":["Job"],"tags":[]},{"location":"job/#ondrop","level":3,"title":"<code>onDrop</code>","text":"<p>Called each time a job record is removed from the database (after execution, or after <code>dropJobId</code>). <code>lastJob</code> is <code>true</code> when this was the last remaining job of this name. <pre><code>override fun onDrop(jobId: String, lastJob: Boolean) { }\n</code></pre></p>","path":["Job"],"tags":[]},{"location":"job/#onlascycledrop","level":3,"title":"<code>onLasCycleDrop</code>","text":"<p>Called when the final cycle of a bounded job (<code>maxCycles</code> or <code>endTime</code>) is reached and the job is dropped. <pre><code>override fun onLasCycleDrop(jobId: String, params: Map&lt;String, Any&gt;) { }\n</code></pre></p>","path":["Job"],"tags":[]},{"location":"job/#periodicjobloaded","level":3,"title":"<code>periodicJobLoaded</code>","text":"<p>Called when the next occurrence of a repeating job is inserted into the database (eagerly, before the current cycle finishes). Use this to track the latest job ID if needed. <pre><code>override fun periodicJobLoaded(originJobId: String, nextJobId: String) { }\n</code></pre></p>","path":["Job"],"tags":[]},{"location":"job/#retries","level":2,"title":"Retries","text":"<p>Set <code>retries</code> to automatically retry a failed execution:</p> <pre><code>object SendReport : Job {\n    override val retries = 3  // try up to 3 additional times after the first failure\n}\n</code></pre> <p>Execution order on repeated failure: 1. <code>execute()</code> → <code>false</code> 2. <code>onRetryFail(retryCount = 1, ...)</code> 3. <code>execute()</code> → <code>false</code> 4. <code>onRetryFail(retryCount = 2, ...)</code> 5. <code>execute()</code> → <code>false</code> 6. <code>onFail(...)</code> ← called only after all retries are exhausted</p>","path":["Job"],"tags":[]},{"location":"job/#blocking-execution","level":2,"title":"Blocking Execution","text":"<p>Override <code>challengeRun</code> to veto execution at runtime (after Kronos's own validation):</p> <pre><code>override fun challengeRun(cycleNumber: Int, params: Map&lt;String, Any&gt;): Boolean {\n    return isPublicHoliday()  // return true to skip this cycle\n}\n</code></pre> <p>The job is skipped for this tick if <code>challengeRun</code> returns <code>true</code>.</p>","path":["Job"],"tags":[]},{"location":"job/runner/","level":1,"title":"Runner","text":"<p>The runner is a background coroutine that ticks every minute, fetches jobs that are due, and dispatches them for execution.</p>","path":["Job","Runner"],"tags":[]},{"location":"job/runner/#overshotaction","level":2,"title":"OvershotAction","text":"<p>When Kronos finds a job whose scheduled time has already passed (e.g. after a server restart), <code>OvershotAction</code> controls what happens:</p> Value Behaviour <code>Drop</code> Job is deleted immediately. (default) <code>Fire</code> Job runs immediately, ignoring the missed window. <code>Nothing</code> Job is left in the database untouched. Avoid this — stale jobs accumulate. <pre><code>Kronos.schedule(\n    jobName = \"my-job\",\n    params = emptyMap(),\n    overshotAction = OvershotAction.Fire\n)\n</code></pre>","path":["Job","Runner"],"tags":[]},{"location":"job/runner/#distributed-locking","level":2,"title":"Distributed Locking","text":"<p>Each job record holds a <code>locks</code> counter. When an instance begins executing a job, it increments the counter atomically. The runner only dispatches jobs where <code>locks == 0</code>, so a job already being processed by one instance is skipped by others.</p> <p>This is a best-effort guard. For strict once-only guarantees in high-concurrency environments, make your <code>execute</code> implementation idempotent.</p>","path":["Job","Runner"],"tags":[]},{"location":"job/runner/#timing","level":2,"title":"Timing","text":"<ul> <li>The runner ticks every 60 seconds.</li> <li>A job scheduled for \"now\" may wait up to 60 seconds before its first tick, unless it was scheduled within the current minute window (in which case Kronos fires it eagerly on insertion).</li> <li>Minimum scheduling resolution is 1 minute.</li> </ul>","path":["Job","Runner"],"tags":[]},{"location":"schedule/","level":1,"title":"Scheduling","text":"<p>Kronos provides two scheduling modes: interval-based and periodic (cron-like).</p>","path":["Schedule","Scheduling"],"tags":[]},{"location":"schedule/#schedule-interval-or-one-time","level":2,"title":"<code>schedule</code> — interval or one-time","text":"<pre><code>suspend fun Kronos.schedule(\n    jobName: String,\n    delay: Duration = Duration.ZERO,   // how long to wait before the first run\n    interval: Duration? = null,        // repeat every N minutes (null = one-time)\n    endTime: Long? = null,             // stop repeating after this epoch-ms timestamp\n    maxCycles: Int? = null,            // stop after N executions\n    retries: Int? = null,              // overrides Job.retries\n    params: Map&lt;String, String&gt;,\n    overshotAction: OvershotAction = OvershotAction.Drop,\n): String?\n</code></pre> <p>Use <code>startTime: Long</code> instead of <code>delay</code> for an absolute UTC epoch-ms start time:</p> <pre><code>Kronos.schedule(jobName = \"my-job\", startTime = epochMs, params = emptyMap())\n</code></pre>","path":["Schedule","Scheduling"],"tags":[]},{"location":"schedule/#examples","level":3,"title":"Examples","text":"<pre><code>// Run once, immediately\nKronos.schedule(jobName = \"my-job\", params = emptyMap())\n\n// Run once in 1 hour\nKronos.schedule(jobName = \"my-job\", delay = 1.hours, params = emptyMap())\n\n// Repeat every 15 minutes, stop after 4 runs\nKronos.schedule(\n    jobName = \"my-job\",\n    interval = 15.minutes,\n    maxCycles = 4,\n    params = emptyMap()\n)\n\n// Repeat every hour until a deadline\nKronos.schedule(\n    jobName = \"my-job\",\n    interval = 1.hours,\n    endTime = deadlineEpochMs,\n    params = emptyMap()\n)\n</code></pre>","path":["Schedule","Scheduling"],"tags":[]},{"location":"schedule/#scheduleperiodic-calendar-aligned","level":2,"title":"<code>schedulePeriodic</code> — calendar-aligned","text":"<p>For jobs that must run at a specific time on a calendar pattern. See Periodic for all options.</p> <pre><code>// Every Monday at 08:00 UTC\nKronos.schedulePeriodic(\n    jobName = \"my-job\",\n    periodic = Periodic.everyWeek(dayOfWeek = 1, hour = 8, minute = 0),\n    params = emptyMap()\n)\n</code></pre>","path":["Schedule","Scheduling"],"tags":[]},{"location":"schedule/#notes","level":2,"title":"Notes","text":"<ul> <li>Minimum resolution is 1 minute — sub-minute scheduling is not supported.</li> <li>All times are UTC. Adjust <code>hour</code>/<code>minute</code> for your timezone.</li> <li><code>cycleNumber</code> is injected into <code>params</code> automatically (key <code>\"cycleNumber\"</code>). Do not set it manually.</li> <li>If both <code>interval</code> and <code>maxCycles</code> are set, the job stops when either limit is reached first.</li> </ul>","path":["Schedule","Scheduling"],"tags":[]},{"location":"schedule/periodic/","level":1,"title":"Periodic Schedules","text":"<p><code>Periodic</code> lets you express calendar-aligned schedules without cron syntax.</p> <p>All times are UTC. If your target time is in another timezone, subtract the UTC offset before passing <code>hour</code>.</p>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#available-patterns","level":2,"title":"Available Patterns","text":"","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#every-minute","level":3,"title":"Every minute","text":"<pre><code>Periodic.everyMinute()\n</code></pre>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#every-hour-at-a-fixed-minute","level":3,"title":"Every hour at a fixed minute","text":"<pre><code>Periodic.everyHour(minute = 30)   // runs at :30 past every hour\n</code></pre>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#every-day-at-a-fixed-time","level":3,"title":"Every day at a fixed time","text":"<p><pre><code>Periodic.everyDay(hour = 9, minute = 0)   // 09:00 UTC daily\n</code></pre> <code>hour</code> is 0–23 (24-hour format).</p>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#every-week-on-a-specific-day","level":3,"title":"Every week on a specific day","text":"<p><pre><code>Periodic.everyWeek(dayOfWeek = 1, hour = 9, minute = 0)  // Monday 09:00 UTC\n</code></pre> <code>dayOfWeek</code>: 1 = Monday … 7 = Sunday.</p>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#every-month-on-a-specific-day","level":3,"title":"Every month on a specific day","text":"<p><pre><code>Periodic.everyMonth(dayOfMonth = 1, hour = 9, minute = 0)  // 1st of every month\n</code></pre> <code>dayOfMonth</code>: 1–31. Jobs scheduled for days that don't exist in a given month (e.g. day 31 in February) will be skipped that month.</p>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#every-year-on-a-specific-date","level":3,"title":"Every year on a specific date","text":"<p><pre><code>Periodic.everyYear(month = 1, dayOfMonth = 1, hour = 0, minute = 0)  // Jan 1 midnight UTC\n</code></pre> <code>month</code>: 1 = January … 12 = December.</p>","path":["Schedule","Periodic Schedules"],"tags":[]},{"location":"schedule/periodic/#usage","level":2,"title":"Usage","text":"<pre><code>Kronos.schedulePeriodic(\n    jobName = \"monthly-report\",\n    periodic = Periodic.everyMonth(dayOfMonth = 1, hour = 8, minute = 0),\n    params = mapOf(\"type\" to \"full\"),\n)\n</code></pre> <p>Optional bounds:</p> <pre><code>Kronos.schedulePeriodic(\n    jobName = \"daily-sync\",\n    periodic = Periodic.everyDay(hour = 2, minute = 0),\n    maxCycles = 30,                    // stop after 30 runs\n    endTime = someDeadlineEpochMs,     // or stop at a specific time\n    params = emptyMap()\n)\n</code></pre>","path":["Schedule","Periodic Schedules"],"tags":[]}]}